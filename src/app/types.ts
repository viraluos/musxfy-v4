export type SidebarLink = {
    name: string;
    path: string;
    icon_path: string;
};

export type SidebarSong = {
    id: string;
    title: string;
    author: string;
    image: string;
    song_path: string;
    duration: string;
}